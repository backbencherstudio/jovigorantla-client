// This hook is no longer used to prevent conflicts with React Router's ScrollRestoration
// Individual pages handle their own scroll restoration using sessionStorage and location.state
// React Router's built-in ScrollRestoration component is now used for global scroll management

export default function useScrollRestoration() {
  // Hook is disabled to prevent conflicts with React Router's ScrollRestoration
  return;
}
